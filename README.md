# Учетные записи — Форма управления

Тестовое задание: форма управления учетными записями на Vue 3 + TypeScript + Pinia + Element Plus.

![Vue](https://img.shields.io/badge/Vue-3.4-4FC08D?logo=vuedotjs&logoColor=white)
![TypeScript](https://img.shields.io/badge/TypeScript-5.4-3178C6?logo=typescript&logoColor=white)
![Pinia](https://img.shields.io/badge/Pinia-2.1-yellow?logo=vue.js&logoColor=white)
![Element Plus](https://img.shields.io/badge/Element%20Plus-2.6-409EFF?logo=element&logoColor=white)
![Vite](https://img.shields.io/badge/Vite-5.1-646CFF?logo=vite&logoColor=white)

## 🚀 Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build
```

## 📋 Функционал

### Основные возможности

- ✅ Добавление новых учетных записей (кнопка "+")
- ✅ Редактирование существующих записей
- ✅ Удаление записей
- ✅ Валидация полей в реальном времени
- ✅ Сохранение данных в localStorage (персистентность)

### Поля учетной записи

| Поле | Тип | Обязательное | Ограничения |
|------|-----|--------------|-------------|
| **Метки** | Текст | Нет | Макс. 50 символов, разделитель `;` |
| **Тип записи** | Select | Да | "Локальная" или "LDAP" |
| **Логин** | Текст | Да | Макс. 100 символов |
| **Пароль** | Password | Да* | Макс. 100 символов |

> \* Пароль обязателен только для типа "Локальная". При выборе "LDAP" поле скрывается.

### Валидация

- **Метки**: необязательны, но если заполнены — максимум 50 символов
- **Логин**: обязателен, не пустой после trim, максимум 100 символов
- **Пароль**: обязателен для "Локальная", максимум 100 символов

Валидация срабатывает:
- Для текстовых полей: на blur (потеря фокуса)
- Для select: на change (смена значения)

Невалидные поля подсвечиваются красной обводкой.

## 🏗️ Архитектура

```
src/
├── app/
│   ├── App.vue          # Корневой компонент
│   └── main.ts          # Точка входа
├── pages/
│   └── AccountsPage.vue # Страница учетных записей
├── components/
│   └── accounts/
│       ├── AccountsList.vue  # Список записей
│       └── AccountRow.vue    # Строка записи
├── stores/
│   └── accounts.store.ts     # Pinia store
├── types/
│   └── accounts.ts           # TypeScript типы
└── utils/
    ├── tags.ts               # Парсинг меток
    └── validate.ts           # Валидация полей
```

## 📦 Типы данных

### StoredAccount (для хранения)

```typescript
interface StoredAccount {
  id: string
  tags: Tag[]              // [{ text: "метка1" }, { text: "метка2" }]
  type: 'LDAP' | 'Локальная'
  login: string
  password: string | null  // null для LDAP
}
```

### AccountRowModel (для UI)

```typescript
interface AccountRowModel {
  id: string
  tagsInput: string        // "метка1; метка2"
  type: 'LDAP' | 'Локальная'
  login: string
  password: string
  errors: {
    tags?: boolean
    login?: boolean
    password?: boolean
  }
}
```

## 💾 Хранение данных

Данные сохраняются в `localStorage` под ключом `accounts_store_v1`.

- При загрузке страницы данные восстанавливаются из localStorage
- При успешной валидации запись автоматически сохраняется
- Невалидные записи НЕ сохраняются в store

## 🛠️ Стек технологий

- **Vue 3** — Composition API, `<script setup>`
- **TypeScript** — строгая типизация
- **Pinia** — state management
- **Element Plus** — UI-компоненты
- **Vite** — сборка и dev-сервер

## 📝 Логика работы

1. **Добавление записи**: нажатие "+" создает пустую UI-строку (не сохраняется в store)
2. **Редактирование**: при blur/change валидируется строка
3. **Сохранение**: если валидно — запись сохраняется в Pinia store и localStorage
4. **Ошибки**: если невалидно — поля подсвечиваются красным, запись НЕ сохраняется
5. **Удаление**: удаляет строку из UI и из store

## 📄 Лицензия

MIT

